[project]
name = "chip-8"
version = "0.1.0"
description = "A simple chip-8 emulator that can run applications through the command line"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pygame==2.6.1",
    "pynput>=1.8.1",
]
